ru:
  errors:
    or: "или"
    and: 'и'
    array?: "должен быть массив"

    empty?: "должно быть пустое значение"

    excludes?: "не может включать %{value}"

    excluded_from?:
      arg:
        default: "не может быть одним из: %{list}"
        range: "не может быть одним из: %{list_left} - %{list_right}"
    exclusion?: "не может быть одним из: %{list}"

    eql?: "должно быть равно %{left}"

    not_eql?: "недолжно быть равно %{left}"

    filled?: "должно быть заполнено"

    format?: "в неверном формате"

    number?: "требуется число"

    odd?: "требуется нечетное число"

    even?: "требуется четное число"

    gt?: "должно быть больше чем %{num}"

    gteq?: "должно быть больше или равно %{num}"

    hash?: "требуется хэш"

    included_in?:
      arg:
        default: "должно быть одним из: %{list}"
        range: "должно быть одним из: %{list_left} - %{list_right}"
    inclusion?: "должно быть одним из: %{list}"

    includes?: "должно включать %{value}"

    bool?: "должно быть логическим значением"

    true?: "должно иметь логическое значение правда"

    false?: "должно иметь логическое значение ложь"

    int?: "должно быть целым числом"

    float?: "должно быть дробным числом"

    decimal?: "должно быть десятичным числом"

    date?: "должно быть датой"

    date_time?: "должно быть датой и временем"

    time?: "должно быть временем"

    key?: "отсутствует"

    attr?: "отсутствует"

    lt?: "должно быть меньше чем %{num}"

    lteq?: "должно быть меньше или равно %{num}"

    max_size?: "размер не может быть больше чем %{num}"

    min_size?: "размер не может быть меньше чем %{num}"

    none?: "не может быть определено"

    str?: "должно быть строкой"

    type?: "должно быть %{type}"

    size?:
      arg:
        default: "размер должен быть %{size}"
        range: "размер должен быть %{size_left} - %{size_right}"

      value:
        string:
          arg:
            default: "длина должна быть %{size}"
            range: "длина должна быть %{size_left} - %{size_right}"

    rules:
      password_confirmation:
        eql?: 'должно совпадать с паролем'
    
    unique?: 'занято'